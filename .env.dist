# .env.example

# Database Credentials
POSTGRES_USER=user
POSTGRES_PASSWORD=
POSTGRES_DB=mydatabase

# Backend Connection String (built using the credentials above)
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}